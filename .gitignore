# Secrets — never commit real env files
.env
.env.*
!.env.example
stack.env

# Dependencies
node_modules

# Runtime data / databases
data
backups
*.db
*.db-wal
*.db-shm

# Logs
npm-debug.log*
logs/

# Private keys / certificates — never commit
*.pem
*.key
*.p12
*.pfx
*.crt

# Local Claude Code tooling state (agent worktrees, etc.)
.claude/
